---
import type { Tutorial } from '~/types';

export type Props = {
  tutorial: Tutorial;
};

const { tutorial } = Astro.props;
---

<div class="py-4 pt-8 sm:py-6">
  <div class="flex flex-col gap-2 mb-4 text-center">
    <h3 id={tutorial.title} style={{ color: tutorial.color }} class="text-4xl font-bold">
      {tutorial.title}
    </h3>
    {
      tutorial.playlistUrl && (
        <a href={tutorial.playlistUrl} target={'_blank'}>
          Watch the full playlist »
        </a>
      )
    }
  </div>
  <div class="grid sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
    <slot />
  </div>
</div>
