---
import NewsletterForm from '../widgets/NewsletterForm.astro';
import { Image } from 'astro:assets';
import stop from './stop.png';

export type Props = {
  issueId: number;
};

const { issueId } = Astro.props;
---

<div class="flex flex-col items-center gap-4 text-center">
  <div class="flex gap-4 items-center text-3xl">
    👇
    <Image src={stop} alt="Leonardo Montini" class="size-22" width={100} height={100} />
    👇
  </div>
  <div class="w-full dark:prose-invert prose-a:text-primary dark:prose-a:text-blue-400">
    What you just read was issue <strong>#{issueId}</strong> of my newsletter. If you enjoyed it, consider subscribing to
    get the next issues straight into your inbox.<br /><br />👇 Just fill the form below 👇
    <NewsletterForm />
  </div>
</div>
